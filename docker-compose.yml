services:
  app:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DB_PATH=/data/app.db
      - GIN_MODE=release
      - PORT=8080
    volumes:
      - ./data:/data
    restart: unless-stopped